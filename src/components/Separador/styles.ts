import { StyleSheet } from "react-native"
import { Separador } from "."


export const styles = StyleSheet.create({
    separador:{
        height:1,
        width:'100%',
        marginTop:5,
        marginBottom: 15
    }
})